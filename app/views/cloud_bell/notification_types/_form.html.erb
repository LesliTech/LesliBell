<%= form_with(model: notification_type, local: true) do |form| %>
  <% if notification_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notification_type.errors.count, "error") %> prohibited this notification_type from being saved:</h2>

      <ul>
        <% notification_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
